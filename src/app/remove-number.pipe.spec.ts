import { RemoveNumberPipe } from './remove-number.pipe';

describe('RemoveNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
